---
- include: yum.yml
  when: ansible_os_family == 'RedHat'

- include: macos.yml
  when: ansible_os_family == 'Darwin'

- include: else.yml
  when: ansible_os_family != 'RedHat' and ansible_os_family != 'Darwin'
